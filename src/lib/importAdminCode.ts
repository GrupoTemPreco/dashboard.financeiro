/** Mesmo padrão da área de importação (App.tsx). */
export const IMPORT_ADMIN_CODE = import.meta.env.VITE_IMPORT_ADMIN_CODE || 'admin123';

/** sessionStorage: dedupeKeys de alertas de plano de contas dispensados pelo admin */
export const CAP_COA_MATCH_DISMISSED_STORAGE_KEY = 'cap_coa_match_dismissed_dedupe_keys';
